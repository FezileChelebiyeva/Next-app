import Apply from "@/components/pages/site/apply/Apply";
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Apply",
    description: "-",
  };

const ApplyPage = () => <Apply />;

export default ApplyPage;
